package by.valuesoft;

import java.util.ArrayList;

public class Data {

    public static void storeAsHTML(ArrayList<League> leagues) throws Exception {
        if (leagues == null)
            throw new Exception("Failed to parse data " + Consts.TASK);


    }

    // push to TG, ws, etc...
}
